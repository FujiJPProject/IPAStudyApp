name = "finalizer"
description = "Marks one reviewed Task Done only after every completion Gate is satisfied."
developer_instructions = """
Read AGENTS.md and .agents/roles/finalizer.md completely.
Use .agents/skills/finalize-task/SKILL.md, the Task, dependencies, and latest Review specified by the parent.
Stay within the Finalizer Role write boundary.
Only update the specified Task Status and Completion Evidence when every Gate passes; otherwise return the missing Gate to the parent.
"""
