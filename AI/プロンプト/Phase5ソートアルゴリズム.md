/goal $orchestrate-feature-cycle を使用し、次の機能変更を計画GateからTask完了確定まで進めてください。

変更要求:
添付の `sort_algorithm_visualizer.html` を参照し、
ソートアルゴリズム可視化教材の追加または既存実装への反映を進めてください。

参照HTMLから、画面構成、情報配置、操作フロー、レスポンシブ表示、
学習体験上の意図を抽出してください。

特に、以下の観点を確認対象に含めてください。

- 配列をバーで表示し、比較・交換・確定状態を色分けする
- 色の意味を画面上で明示する
- 1ステップ実行、自動再生、一時停止、速度変更、リスタート、シャッフルを提供する
- アルゴリズムを切り替えられる
- 配列表示エリアで変数名や処理中の値を確認できる
- 初期状態、処理開始、ステップ実行、状態変化、完了結果、解説の流れを理解できる
- フローチャートは可読性を優先し、適切な場合のみ開始・終了、分岐、反復を図形で表現する
- PCとスマートフォンの両方で破綻しない

添付HTMLはUI確認用プロトタイプです。
HTML / CSS / JavaScriptの内部実装を、そのまま本番実装へ流用する前提にはしないでください。
既存のSource of Truth、Task、実装規約との相違点を示し、
仕様として採用する必要がある内容はユーザーが確定するまで変更しないでください。

Candidate Reference:
sort_algorithm_visualizer.html

対応する既存Task:
親が確認

重要なユーザー判断が必要な場合は、
質問を重複排除して提示し、回答を受けるまで停止してください。

完了条件:

- 対象TaskがDone
- Completion Evidenceが記録されている
- 最新Reviewが現在の実装を対象としている
- 最新ReviewがNext step: proceedで終了している
- npm run testとnpm run buildが成功している

push、PR作成、merge、deployは行わないでください。
